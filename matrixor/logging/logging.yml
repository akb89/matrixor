version: 1
formatters:
  brief:
    format: '%(levelname)s - %(message)s'
  simple:
    format: '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
handlers:
  console:
    class: logging.StreamHandler
    formatter: simple
loggers:
  matrixor.main:
    level: INFO
    handlers: [console]
  matrixor.utils:
    level: INFO
    handlers: [console]
  matrixor.transformation:
    level: INFO
    handlers: [console]
